# package.json declares node >=20; fail fast instead of installing on an
# older runtime and discovering it at build time.
engine-strict=true

# The committed dist/ bundle must be reproducible, so keep installs exact.
save-exact=false
audit-level=moderate
fund=false
